export const SET_ORDER_DATA = (state , data ) => {
    state.OrderData = data;
}

export const SET_ORDER_CREATE_UPDATE_DATA = (state , data ) => {
    state.orderCreateUpdateData = data;
}

export const SET_ORDERS_DATA = (state , data ) => {
    state.OrdersData = data;
}

export const SET_ORDER_DATE = (state , data ) => {
    state.orderDateTime = data;
}
