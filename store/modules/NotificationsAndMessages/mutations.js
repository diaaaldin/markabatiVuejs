export const SET_NOTIFICATION_CREATED_DATA = (state , data ) => {
    state.NotificationData = data;
}

export const SET_USER_NOTIFICATIONS_DATA = (state , data ) => {
    state.getUserNotificationsData = data;
}

